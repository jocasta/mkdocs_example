# code-examples
# Function to add two numbers

```python title="add_numbers.py" linenums="1"
def add_two_numbers(num1, num2):
    return num1 + num2
```

<br>
<hr>

# Example usage

```python title="add_numbers.py" hl_lines="4-5"
result = add_two_numbers(5, 3)
print('The sum is:', result)

result = add_two_numbers(5, 3)
print('The sum is:', result)
```